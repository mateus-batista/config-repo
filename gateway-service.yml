spring:
  eureka:
    client:
      serviceUrl:
        defaultZone: http://eureka_client:secret@localhost:8761/eureka/
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: greeting-instance
          uri: lb://greeting-instance
          predicates:
            - Path=/greeting/**

---

spring:
  profiles: docker
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka_client:secret@eureka-service:8761/eureka/
